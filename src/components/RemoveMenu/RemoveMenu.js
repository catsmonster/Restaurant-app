import React from 'react';

const RemoveMenu = ({name, clickedTable}) => {
    return (
        <div onClick={()=> {

        }}>
            {name}
        </div>
    );
};

export default RemoveMenu;