import React from 'react';

const UpdateOrder = ({name, onRemoveOrderedItem}) => {
    return (
        <div>
            <button className='addTable'>Update price</button>
            <button className='addTable'>Add comment</button>
        </div>
    )
};

export default UpdateOrder;